import figura
import math
class circulo():

    def calcularRadio():
        radio = (origen.calcularDistancia(fin)/2)
        return radio
 
    def calcularArea():
        area = (math.pi*(calcularRadio()**2))

    def calcularPerimetro():
        lado = origen.calcularDistancia(fin)
        perimetro = (2*math.pi*calcularRadio())
    
