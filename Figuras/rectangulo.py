import interfaz
class rectangulo():
   
    def calcularArea(): 
        temp=punto.Punto()
        temp.setX(origen.x)
        temp.setY(fin.y)
        base = temp.calcularDistancia(fin)
        altura= origen.calcularDistancia(temp)
        area = base * altura
    

    def calcularPerimetro(): 
        temp=punto.Punto()
        temp.setX(origen.x)
        temp.setY(fin.y)
        base = temp.calcularDistancia(fin)
        altura= origen.calcularDistancia(temp)
        perimetro =  base + altura
    
